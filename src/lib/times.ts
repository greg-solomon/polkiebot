const SECOND = 1000;
const MINUTE = 60 * SECOND;
const HOUR = 60 * MINUTE;
const DAY = 24 * HOUR;
const WEEK = 7 * DAY;
const times = {
  SECOND,
  MINUTE,
  HOUR,
  DAY,
  WEEK
}

export default times;